# =============================================================================
# DITTAH 3.0 - Environment Configuration
# =============================================================================
# Copy this file to .env and configure your values:
#   cp .env.example .env
#   nano .env
#
# SETUP PROFILES (selected in Setup Wizard):
#   Light:      8GB RAM  - Local Ollama (llama3.2:3b)
#   Medium:     16GB RAM - Local Ollama (qwen2.5:7b-instruct) [Recommended]
#   Production: Cloud APIs (Groq) - requires API key
# =============================================================================

# =============================================================================
# REQUIRED SETTINGS
# =============================================================================

# PostgreSQL root password (REQUIRED)
# Note: install.sh auto-generates POSTGRES_PASSWORD and ARTEMIS_PASSWORD
POSTGRES_PASSWORD=your_secure_password_here

# =============================================================================
# DATABASE SETTINGS (optional - defaults shown)
# =============================================================================

# PostgreSQL
POSTGRES_USER=postgres
POSTGRES_DB=app
POSTGRES_PORT=5432

# Application database users (created by init scripts)
DB_APP_USER=cfappusr
DB_APP_PASSWORD=auto_generated_by_install
DB_AUTH_USER=cfauthappusr
DB_AUTH_PASSWORD=auto_generated_by_install

# =============================================================================
# MESSAGE QUEUE SETTINGS (optional - defaults shown)
# =============================================================================

ARTEMIS_USER=artemis
ARTEMIS_PASSWORD=auto_generated_by_install
ARTEMIS_PORT=61616
ARTEMIS_CONSOLE_PORT=8161

# =============================================================================
# APPLICATION SETTINGS (optional - defaults shown)
# =============================================================================

# Edition: ENTERPRISE or COMMUNITY
DITTAH_EDITION=COMMUNITY

# Image registry and version
DITTAH_IMAGE_REGISTRY=dittah
DITTAH_VERSION=1.0.0

# Service ports
REST_SERVER_PORT=8080
UI_PORT=4200

# CORS origins (comma-separated)
CORS_ORIGINS=http://localhost:4200,http://localhost:8081

# Backend URL (for UI to connect to API)
BACKEND_URL=http://localhost:8080

# =============================================================================
# OLLAMA SETTINGS (for Light/Medium profiles)
# =============================================================================
# Ollama runs on the host machine (not in Docker).
# Install from https://ollama.com/ and it will listen on port 11434 by default.
# The containers connect to it via host.docker.internal:11434.

# =============================================================================
# SUPPORT TOOLS (optional - for --profile support)
# =============================================================================

PGADMIN_EMAIL=admin@dittah.local
PGADMIN_PASSWORD=admin
PGADMIN_PORT=5051
